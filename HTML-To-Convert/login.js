// JavaScript source code

document.addEventListener("DOMContentLoaded", activateBtns);

function activateBtns{

  document.getElementById("Pro").addEventListener("click", function (event) {
    // trigger the Profession filter when searching for an expert.
  }

    document.getElementById("Code").addEventListener("click", function (event) {
    // trigger the Code filter when searching for an expert
  }

    document.getElementById("Class").addEventListener("click", function (event) {
    // trigger the class filter when searching for an expert
  }

    document.getElementById("Org").addEventListener("click", function (event) {
    // trigger the Organization filter when searching for an expert
  }


    document.getElementById("Sign").addEventListener("click", function (event) {
    // trigger the Sign in button, and let the User sign into their account
  }

    document.getElementById("loginBtn").addEventListener("click", function (event) {
    // trigger the login button, and let the User log into their account
  }

    document.getElementById("signBtn").addEventListener("click", function (event) {
    // trigger the Sign in button, and let the User sign up for an account .
  }

}
